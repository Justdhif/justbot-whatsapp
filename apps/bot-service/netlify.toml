[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "functions"
  node_bundler = "esbuild"
  external_node_modules = ["brat-canvas", "@napi-rs/canvas", "sharp", "iqc-canvas", "emoji-db"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
